var addToArrayForm = function (A, K) {
  let returnArr = function (index, key) {
      let sums = parseInt(A[index]) + parseInt(key)
      A[index] = sums % 10

      if ()
  }
  returnArr(A.length - 1, K)
  console.log(A)
};


addToArrayForm([2, 7, 4], 181 )
